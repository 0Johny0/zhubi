version: "3.8"

services:
  zhubi:
    build: .
    container_name: zhubi
    restart: unless-stopped
    ports:
      - "8321:8321"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
